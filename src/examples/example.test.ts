import { expect, test } from 'vitest';

test('Example for run a test', () => {
    // Operations
    const statusCodeResult = 404;

    // Validation
    expect(statusCodeResult).toEqual(404);
});